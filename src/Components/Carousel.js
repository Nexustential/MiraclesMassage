import "../Styles/Carousel.css";

const Carousel = () => {
  return (
    <div className="carousel-container">
      <h1>Carousel</h1> 
      {/* It's weird how the top margin collapses when you delete the h1 element above */}
    </div>
  );
};

export default Carousel;
